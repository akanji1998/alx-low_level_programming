#include "main.h"
/**
 * add - do the addition of two number
 * @a: the first number
 * @b: the second number
 * Return: the result(the addition)
 */
int add(int a, int b)
{
	int result;

	result = a + b;
	_putchar(result);

	return (result);
}
